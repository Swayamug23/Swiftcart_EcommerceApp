import React from 'react'

export const metadata = {
  title: "Users",
  description: "Browse and manage users of the platform.",
};

const layout = ({children}) => {
  return (
    <div>
        {children}
    </div>
  )
}

export default layout